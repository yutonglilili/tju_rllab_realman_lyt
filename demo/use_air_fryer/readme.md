本文件夹下脚本目前针对空气炸锅任务特调。
任务流程如下：
1. 拉开空气炸锅；
2. 将盘子中的红薯放到空气炸锅中；
3. 合上空气炸锅；
4. 定时。
实现方法：
1. 写一个 hack 函数库，里面的函数根据打点转换的世界坐标，进行特定的动作，如推，拉，旋转。
2. 在 main 函数里，写死流程。
    - 先对空气炸锅的把手打点，调用 pull 函数；
    - 调用现有 pnp 函数将红薯放到锅中；
    - 再对空气炸锅把手打点，调用 push函数；
    - 对旋钮打点，调用 roll 函数；
    - reset。